Feature: Status — dry-run preview of sync changes

  `beehave status` performs the same analysis as `sync` but makes no changes.
  It reports what would be created, updated, or warned about, allowing
  developers to review the impact before running `beehave sync`.

  Status: DRAFT
