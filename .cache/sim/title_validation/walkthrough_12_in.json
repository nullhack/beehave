{
  "walkthrough": 12,
  "description": "Rule title matches Feature title — cross-type duplicate",
  "type": "edge_case",
  "state": {
    "features_dir": "docs/features",
    "files": [
      {
        "path": "docs/features/hive_activity.feature",
        "titles": {
          "Feature": "Hive Activity",
          "Rule": ["Hive Activity"],
          "Scenario": ["guard bee inspects visitor"]
        }
      }
    ],
    "note": "Rule title 'Hive Activity' matches Feature title 'Hive Activity' case-sensitive (same casing). Cross-type duplicate: Rule vs Feature. duplicate-rule-title should be produced because the Rule title is the duplicate one (matching a Feature)."
  },
  "stimulus": {
    "call": "validate_all_titles(config)",
    "contract": "Feature Parsing validates uniqueness: Rule duplicate matches any Feature, Rule, or Scenario title"
  }
}
