{
  "walkthrough": 15,
  "description": "M1/M6: Exit code 2 — fatal error (features_dir missing)",
  "type": "error_path",
  "state": {
    "project_state": {
      "features_dir": "nonexistent_dir",
      "tests_dir": "tests/features",
      "note": "Config loaded successfully from pyproject.toml with features_dir set to 'nonexistent_dir'. The directory does not exist on disk."
    },
    "note": "Before computing any feature statuses, Status Reporting must verify that features_dir exists. This is a fatal precondition check."
  },
  "stimulus": {
    "call": "beehave status",
    "contract": "Exit Code Contract: exit 2 on fatal error. Preconditions: features_dir must be a resolveable path that exists.",
    "question": "Does the CLI detect the nonexistent features_dir and exit 2 before producing any output?"
  }
}
