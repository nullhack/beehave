{
  "walkthrough": 13,
  "description": "M1: Exit code 0 — all features ok",
  "type": "happy_path",
  "state": {
    "project_state": {
      "features_dir": "docs/features",
      "features": [
        {"path": "login", "stage": "ok", "title": "Login"},
        {"path": "signup", "stage": "ok", "title": "Signup"}
      ]
    },
    "note": "Both features have stage 'ok'. The status command must exit 0 per the Exit Code Contract."
  },
  "stimulus": {
    "call": "beehave status",
    "contract": "Exit Code Contract: exit 0 when all features are ok (or zero features)",
    "question": "Does the CLI correctly compute exit code 0 when all features have stage 'ok'?"
  }
}
