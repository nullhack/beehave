{
  "walkthrough": 12,
  "description": "PP-1 verify: Feature with Rules but zero Scenario children → detect_empty_rules() returns has_empty_rules=True → needs scenarios",
  "type": "happy_path",
  "state": {
    "feature_file": "docs/features/draft_rules.feature",
    "file_content": "Feature: Draft Rules\n  Rule: Authentication rules\n  Rule: Authorization rules\n",
    "note": "parse_feature() returns {} because Rule nodes have zero Scenario children. detect_empty_rules() performs lightweight AST pass to inspect Rule→Scenario child counts."
  },
  "stimulus": {
    "call": "beehave.status.compute_feature_status('docs/features/draft_rules.feature')",
    "contract": "Status Reporting evaluates Stage Decision Tree priority order: parse_feature() returns {} → call detect_empty_rules() → has_empty_rules=True → stage 'needs scenarios'",
    "question": "Does the Stage Decision Tree correctly reach priority 3 when has_empty_rules=True?"
  }
}
